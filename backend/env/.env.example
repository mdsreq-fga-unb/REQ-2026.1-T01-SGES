NODE_ENV='dev'
PORT=3000
DEBUG=1

POSTGRES_URL=postgresql://user:pass@port/db-name
SLAVE_POSTGRES_URL=postgresql://user:pass@port/db-name
DATA_SOURCE_POOL_SIZE=

JWT_SECRET=''

REDIS_URL=

# Email (Gmail SMTP) — gere App Password em https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=seu-email@gmail.com
SMTP_PASS=
SMTP_FROM=SGES <noreply@sges.dev>

# Fila assíncrona (opcional — sem Redis usa SyncQueueProducer)
REDIS_URL=